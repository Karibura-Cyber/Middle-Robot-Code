import display
import buzzer
import switch
import random
import time

answer = []
user_input = []

point = 0

def waitSwitch():
    while True:
        if switch.value(switch.S1):
            while switch.value(switch.S1):
                pass
            return 1
        
        if switch.value(switch.S2):
            while switch.value(switch.S2):
                pass
            return 2

while True:
    answer.append(random.randint(1, 2)) # Random number from 1 - 2 then assign to answer
    user_input = []     # Clear user_input list

    display.clear()     # Clear display
    time.sleep(1)       # Wait for 1 second

    for value in answer:
        display.show4x8(value)
        buzzer.tone(1500, 0.1)
        time.sleep(0.5)

        display.clear()
        time.sleep(0.5)
    
    
